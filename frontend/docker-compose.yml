version: '3.8'

services:
  bank-auction-properties:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bank-auction-properties
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
